---
phase: 01-recording-and-transcription-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/components/transcript/transcript-view.tsx
  - src/components/transcript/utterance-bubble.tsx
  - src/app/(dashboard)/recordings/[id]/page.tsx
  - src/hooks/use-recordings.ts
autonomous: false
requirements:
  - STT-02
  - STT-03

must_haves:
  truths:
    - "User can click a recording in the Hub and see a detail page"
    - "Detail page shows recording info (title, duration, status, project)"
    - "Completed transcript displays as chat-style speech bubbles"
    - "Each speaker has a distinct color for label and subtle bubble tint"
    - "Each utterance shows inline muted timestamp (e.g. 0:32)"
    - "Speaker labels display as Speaker 1, Speaker 2, etc."
  artifacts:
    - path: "src/components/transcript/transcript-view.tsx"
      provides: "Full transcript display with chat bubbles"
      exports: ["TranscriptView"]
    - path: "src/components/transcript/utterance-bubble.tsx"
      provides: "Single speaker utterance bubble component"
      exports: ["UtteranceBubble"]
    - path: "src/app/(dashboard)/recordings/[id]/page.tsx"
      provides: "Recording detail page with info and transcript"
  key_links:
    - from: "src/app/(dashboard)/recordings/[id]/page.tsx"
      to: "src/hooks/use-recordings.ts"
      via: "useRecording and useTranscript hooks"
      pattern: "useRecording|useTranscript"
    - from: "src/components/transcript/transcript-view.tsx"
      to: "src/components/transcript/utterance-bubble.tsx"
      via: "Maps utterances to UtteranceBubble components"
      pattern: "UtteranceBubble"
    - from: "src/hooks/use-recordings.ts"
      to: "/api/recordings/[id]/transcript"
      via: "TanStack Query fetch"
      pattern: "api/recordings.*transcript"
---

<objective>
Build the recording detail page with chat-style transcript display. After this plan, users can click any recording in the Hub, see its metadata, and read the transcript with speaker-colored bubbles and timestamps. This completes the full Phase 1 pipeline: Record -> Upload -> Transcribe -> View.

Purpose: Delivers the final user-visible output of the transcription pipeline -- the readable transcript.
Output: Recording detail page with chat-bubble transcript at /recordings/[id].
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-recording-and-transcription-pipeline/01-RESEARCH.md
@.planning/phases/01-recording-and-transcription-pipeline/01-CONTEXT.md
@.planning/phases/01-recording-and-transcription-pipeline/01-03-SUMMARY.md

<interfaces>
<!-- Types and hooks from Plans 01, 02, and 03 -->

From src/types/recording.ts:
```typescript
export type RecordingStatus = 'unassigned' | 'processing' | 'ready' | 'error';

export interface Recording {
  id: string;
  title: string;
  durationMs: number;
  filePath: string | null;
  status: RecordingStatus;
  projectId: string | null;
  backendId: string | null;
  errorMessage: string | null;
  createdAt: string;
  updatedAt: string;
}

export interface Utterance {
  id: string;
  speaker: string;
  text: string;
  startTime: number; // seconds
  endTime: number;   // seconds
}

export interface Transcript {
  id: string;
  recordingId: string;
  utterances: Utterance[];
}
```

From src/hooks/use-recordings.ts:
```typescript
export function useRecording(id: string): UseQueryResult<Recording>;
export function useRecordingStatus(id: string, enabled: boolean): UseQueryResult<{status: RecordingStatus}>;
// useTranscript needs to be added in this plan
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
export function formatDuration(ms: number): string;
export function formatTimestamp(seconds: number): string;
```

From src/components/recording/status-badge.tsx:
```typescript
export function StatusBadge({ status }: { status: RecordingStatus }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build transcript components and recording detail page</name>
  <files>src/components/transcript/utterance-bubble.tsx, src/components/transcript/transcript-view.tsx, src/app/(dashboard)/recordings/[id]/page.tsx, src/hooks/use-recordings.ts</files>
  <action>
1. Add `useTranscript` hook to `src/hooks/use-recordings.ts`:
   - `useTranscript(recordingId: string, enabled: boolean)` -- fetches from `/api/recordings/{id}/transcript`
   - Only enabled when recording status is 'ready'
   - Returns `UseQueryResult<Transcript>`
   - Stale time: Infinity (transcripts don't change once ready)

2. Create `src/components/transcript/utterance-bubble.tsx`:
   - Client component
   - Props: `utterance: Utterance`
   - Speaker color palette (5 distinct colors cycling):
     - Speaker 1: blue-50 bg, blue-700 label
     - Speaker 2: emerald-50 bg, emerald-700 label
     - Speaker 3: purple-50 bg, purple-700 label
     - Speaker 4: amber-50 bg, amber-700 label
     - Speaker 5: rose-50 bg, rose-700 label
   - Extract speaker number from "Speaker N" string, map to color index (mod 5)
   - Layout: rounded-lg bubble with max-w-[85%]
     - Top row: speaker label (colored, text-xs font-semibold) + timestamp (text-xs text-muted-foreground, formatted as "M:SS")
     - Body: utterance text (text-sm)
   - Per locked decisions: chat-style speech bubbles, distinct color per speaker, inline muted timestamps

3. Create `src/components/transcript/transcript-view.tsx`:
   - Client component
   - Props: `transcript: Transcript`
   - Renders a vertical list of UtteranceBubble components with gap-3 spacing
   - Group consecutive utterances by same speaker visually (reduce gap between same-speaker bubbles, only show speaker label on first of group)
   - If transcript has no utterances, show empty state: "No transcript content available"
   - Loading state handled by parent (skeleton)

4. Create `src/app/(dashboard)/recordings/[id]/page.tsx`:
   - Client component (needs hooks for data fetching)
   - Uses `useRecording(id)` to fetch recording metadata
   - Uses `useTranscript(id, recording.status === 'ready')` to fetch transcript when ready
   - Uses `useRecordingStatus(id, recording.status === 'processing')` for live polling if still processing
   - Layout:
     - Back button/link to /recordings (breadcrumb or arrow)
     - Header section: recording title (large), date created (muted), duration, StatusBadge
     - Project name if assigned
     - If status is 'processing': show a centered spinner/loading state with "Transcribing..." text
     - If status is 'ready': show TranscriptView with the transcript data
     - If status is 'error': show error message in a destructive alert/callout
     - If status is 'unassigned': show a message like "Assign to a project to begin transcription" with a link back to hub or an inline project assignment dropdown
   - Loading skeleton while recording data loads (use shadcn Skeleton component)
   - Error state if recording not found (404-like page)

5. Fill in test skeletons with real tests:
   - `src/components/transcript/__tests__/transcript-view.test.tsx`: Render with mock transcript data, verify utterance bubbles render, verify timestamps displayed, verify empty state.
   - `src/components/transcript/__tests__/utterance-bubble.test.tsx`: Render with mock utterance, verify speaker label color, verify text content, verify timestamp format, verify color cycling across 5 speakers.

6. Ensure the recordings table rows in recording-hub.tsx from Plan 03 navigate to `/recordings/{id}` on click (this should already be wired, but verify the link works).
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npm run build && npx vitest run src/components/transcript/__tests__/transcript-view.test.tsx src/components/transcript/__tests__/utterance-bubble.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Recording detail page renders at /recordings/[id] with recording metadata. Transcript displays as chat-style bubbles with speaker colors and timestamps when status is 'ready'. Processing state shows spinner. Error state shows message. Unassigned state prompts user to assign project. Transcript and utterance bubble tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 end-to-end pipeline</name>
  <action>Human verifies the full Record -> Hub -> Assign -> Poll -> View Transcript pipeline works end-to-end in the browser.</action>
  <what-built>Complete Phase 1 recording and transcription pipeline: FAB recording, Recording Hub with table/tabs/assignment, backend upload proxy, status polling, and chat-bubble transcript display.</what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Visit http://localhost:3000 -- you should see the dashboard with a FAB in the bottom-right corner
3. Click the FAB -- it should request microphone permission, then show a red pulsing dot with elapsed time
4. Navigate to /recordings while recording -- recording should continue (FAB still shows recording state)
5. Click the FAB again to stop -- toast should say "Recording saved"
6. Go to /recordings -- the Recording Hub should show your recording as "Unassigned" in the table
7. Check tab counts update (All: 1, Unassigned: 1)
8. Click the project dropdown on the recording row -- should show "Create new project" option
9. Create a new project -- recording should immediately show "Processing" status
10. (If Python backend is running) Watch status poll every 3s until "Ready"
11. Click the recording row -- should navigate to detail page
12. (If transcription completed) Verify chat bubbles with speaker colors and timestamps
13. (If backend not running) Verify the Processing state is shown, and detail page shows "Transcribing..." state

Note: Full end-to-end requires the Python FastAPI backend running. Without it, verify:
- Recording and auto-save works (steps 1-6)
- Hub UI with tabs and counts works (steps 7-8)
- Project assignment UI works (step 9)
- Status badge changes on assignment (step 9)
- Detail page renders correctly for unassigned and processing states (steps 11-13)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx vitest run src/components/transcript/__tests__/transcript-view.test.tsx` passes
3. `npx vitest run src/components/transcript/__tests__/utterance-bubble.test.tsx` passes
4. /recordings/[id] page renders with recording metadata and transcript
5. UtteranceBubble uses 5 distinct speaker colors
6. Timestamps formatted as M:SS inline in each bubble
7. Speaker labels show as "Speaker 1", "Speaker 2", etc.
8. TranscriptView handles empty state
9. Detail page shows appropriate state for unassigned/processing/ready/error
10. Full pipeline verified by human: Record -> Hub -> Assign -> Poll -> View Transcript
</verification>

<success_criteria>
- Recording detail page at /recordings/[id] displays recording info and transcript
- Chat-style bubbles with speaker colors (5 color palette) and inline timestamps
- Transcript fetched only when status is 'ready'
- Processing recordings show "Transcribing..." with spinner
- Error recordings show error message
- Unassigned recordings prompt to assign project
- Transcript and utterance bubble tests pass
- Human verifies complete end-to-end flow works
</success_criteria>

<output>
After completion, create `.planning/phases/01-recording-and-transcription-pipeline/01-04-SUMMARY.md`
</output>


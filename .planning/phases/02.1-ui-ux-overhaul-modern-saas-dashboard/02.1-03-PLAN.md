---
phase: 02.1-ui-ux-overhaul-modern-saas-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/components/recording/recording-hub.tsx
  - src/components/recording/recording-fab.tsx
  - src/components/recording/status-badge.tsx
  - src/components/outcome/outcome-card.tsx
  - src/components/outcome/summary-banner.tsx
  - src/components/transcript/utterance-bubble.tsx
  - src/app/(dashboard)/recordings/page.tsx
  - src/app/(dashboard)/recordings/[id]/page.tsx
autonomous: false
requirements:
  - UI-03

must_haves:
  truths:
    - "Recording Hub has a text search bar that filters recordings by title"
    - "Recording Hub has a project dropdown filter"
    - "All tabs, badges, and status indicators use indigo theme"
    - "Outcome cards have refined borders, shadows, and indigo accent"
    - "Transcript bubbles use indigo-complementary speaker colors"
    - "Recording FAB is indigo when idle, red pulsing when recording"
    - "Page headings use Space Grotesk font"
    - "Confidence badges use green (>=0.80) and amber (<0.80) with indigo accents"
  artifacts:
    - path: "src/components/recording/recording-hub.tsx"
      provides: "Recording Hub with search + project filter"
      contains: "Input"
    - path: "src/components/recording/recording-fab.tsx"
      provides: "Indigo-themed FAB"
      contains: "bg-primary"
    - path: "src/components/outcome/outcome-card.tsx"
      provides: "Polish outcome cards with indigo accents"
      contains: "border-primary"
    - path: "src/components/transcript/utterance-bubble.tsx"
      provides: "Indigo-complementary speaker color palette"
      min_lines: 20
  key_links:
    - from: "src/components/recording/recording-hub.tsx"
      to: "src/components/ui/input.tsx"
      via: "Search input component for title filtering"
      pattern: "Input"
    - from: "src/components/recording/recording-hub.tsx"
      to: "src/hooks/use-recordings.ts"
      via: "useRecordings() provides data to filter"
      pattern: "useRecordings"
---

<objective>
Polish all existing views -- Recording Hub (search + filter + theme), Recording Detail (indigo tabs, outcome cards, transcript bubbles), and FAB -- to match the new indigo SaaS aesthetic.

Purpose: Complete the visual transformation by applying the indigo theme and typography to every user-facing component, plus add search/filter functionality to the Recording Hub.
Output: All existing views polished with consistent indigo theme, plus Recording Hub search and filter.
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-ui-ux-overhaul-modern-saas-dashboard/02.1-CONTEXT.md
@.planning/phases/02.1-ui-ux-overhaul-modern-saas-dashboard/02.1-RESEARCH.md
@.planning/phases/02.1-ui-ux-overhaul-modern-saas-dashboard/02.1-01-SUMMARY.md

<interfaces>
<!-- Existing components to modify -->

From src/components/recording/recording-hub.tsx:
```typescript
// Uses: Tabs, TabsList, TabsTrigger, TabsContent, Table, RecordingRow
// Uses: useRecordings, useRecordingStatus, useAssignProject hooks
// Filters: all, unassigned, processing, ready tabs
// Shows counts in tab labels
```

From src/components/recording/recording-fab.tsx:
```typescript
// Fixed bottom-right z-50
// Currently: bg-primary when idle (will now be indigo), bg-red-500 when recording
// Uses useAudioRecorder, useUploadRecording
```

From src/components/recording/status-badge.tsx:
```typescript
// StatusBadge with statusConfig mapping
// ready: bg-green-600 text-white (hardcoded -- needs to use semantic color)
// processing: has ping animation
```

From src/components/outcome/outcome-card.tsx:
```typescript
// border-l-4 with green-500/amber-500 based on confidence
// Confidence badge: bg-green-100/bg-amber-100
// Evidence links: text-blue-600 (should become text-primary)
```

From src/components/transcript/utterance-bubble.tsx:
```typescript
// speakerColors array: blue, emerald, purple, amber, rose backgrounds
// highlighted: bg-yellow-200/60
```

From src/components/ui/input.tsx (installed in Plan 01):
```typescript
export { Input }
// Standard shadcn input with focus ring
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recording Hub search, filter, and theme polish</name>
  <files>src/components/recording/recording-hub.tsx, src/app/(dashboard)/recordings/page.tsx</files>
  <action>
**Modify `src/components/recording/recording-hub.tsx`:**

1. Add search functionality:
   - Import `Input` from `@/components/ui/input`, `Search` from `lucide-react`, and `useState`, `useDeferredValue` from React
   - Add search state: `const [query, setQuery] = useState("")` and `const deferredQuery = useDeferredValue(query)`
   - Filter recordings by title before passing to tab filter: `const searchFiltered = deferredQuery ? allRecordings.filter(r => r.title.toLowerCase().includes(deferredQuery.toLowerCase())) : allRecordings`
   - Use `searchFiltered` instead of `allRecordings` for counts and tab filtering

2. Add project dropdown filter:
   - Import `Select, SelectTrigger, SelectValue, SelectContent, SelectItem` from `@/components/ui/select`
   - Add state: `const [projectFilter, setProjectFilter] = useState<string>("all")`
   - Extract unique projectIds from recordings: `const projects = [...new Set(allRecordings.filter(r => r.projectId).map(r => r.projectId!))]`
   - Apply project filter after search filter: filter recordings where projectFilter === "all" OR recording.projectId === projectFilter
   - Render select dropdown next to search bar with "All Projects" default + each project option

3. Layout the controls:
   - Above tabs, add a flex row: `<div className="flex items-center gap-3">`
   - Search input with Search icon: `<div className="relative flex-1 max-w-sm"><Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" /><Input placeholder="Search recordings..." value={query} onChange={e => setQuery(e.target.value)} className="pl-9" /></div>`
   - Project select dropdown (only render if projects.length > 0)

4. Theme polish:
   - All existing tab, table, badge styles already consume CSS variables so they automatically inherit indigo theme
   - No additional changes needed for tab/table theming -- the CSS variable update in Plan 01 handles it

**Modify `src/app/(dashboard)/recordings/page.tsx`:**
- Add page heading: `<h1 className="text-2xl font-heading font-bold tracking-tight">Recordings</h1>` above the RecordingHub component
- If this page just renders `<RecordingHub />`, add the heading wrapper
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>Recording Hub has search bar filtering recordings by title. Project dropdown filter visible when recordings have projects. Tabs show correct filtered counts. Page heading uses Space Grotesk. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Component polish -- FAB, status badges, outcome cards, transcript bubbles, detail page headings</name>
  <files>src/components/recording/recording-fab.tsx, src/components/recording/status-badge.tsx, src/components/outcome/outcome-card.tsx, src/components/outcome/summary-banner.tsx, src/components/transcript/utterance-bubble.tsx, src/app/(dashboard)/recordings/[id]/page.tsx</files>
  <action>
**recording-fab.tsx:**
- The FAB already uses `bg-primary` when idle (which is now indigo via CSS variables). Verify this works -- no code change needed for idle state.
- For recording state: keep `bg-red-500 animate-pulse` -- red recording indicator is correct.
- Update focus ring to use primary: `focus:ring-primary` (already there). Good -- FAB should work with no changes needed. Verify and leave as-is if already correct.

**status-badge.tsx:**
- Change "ready" config: remove hardcoded `className: "bg-green-600 text-white"` and replace with `className: "bg-emerald-600 text-white"` (green semantic for ready status -- keep this semantic, not indigo). Actually, per CONTEXT.md: "Semantic colors: green for confidence >=0.80, amber for confidence <0.80, red for errors/destructive". Status "ready" is semantic success = keep green. Status "processing" should use indigo tint: add `className: "bg-primary/10 text-primary"` for processing status to use the new indigo theme.
- Update processing config: `variant: "secondary"` to include `className: "bg-primary/10 text-primary border-primary/20"` for indigo processing badge.

**outcome-card.tsx:**
- Change evidence link color from `text-blue-600 hover:text-blue-800` to `text-primary hover:text-primary/80` (use indigo instead of blue)
- Add subtle shadow to the card: change outer div to include `shadow-sm hover:shadow-md transition-shadow`
- Update the "Needs review" text: keep `text-amber-700` (semantic amber for low confidence)
- The left border colors (green-500 / amber-500) are semantic and should stay

**summary-banner.tsx:**
- Update background: change `bg-muted/40` to `bg-primary/5` for subtle indigo tint
- The count number: add `font-heading` to `<span className="text-2xl font-bold">` making it `text-2xl font-heading font-bold`
- "Needs review" badge: keep amber colors (semantic)

**utterance-bubble.tsx:**
- Update speaker colors to complement indigo theme. Replace the speakerColors array:
```typescript
const speakerColors = [
  { bg: "bg-indigo-50", label: "text-indigo-700" },
  { bg: "bg-teal-50", label: "text-teal-700" },
  { bg: "bg-violet-50", label: "text-violet-700" },
  { bg: "bg-amber-50", label: "text-amber-700" },
  { bg: "bg-rose-50", label: "text-rose-700" },
] as const
```
This shifts blue->indigo and emerald->teal to better complement the indigo primary.

**recordings/[id]/page.tsx (detail page):**
- Update page heading h2: add `font-heading` class to `<h2 className="text-2xl font-bold tracking-tight ...">` making it `text-2xl font-heading font-bold tracking-tight`
- Update the "Recording Info" h3: add `font-heading` class
- The Tabs component already uses CSS variables so indigo styling is inherited from Plan 01
- Update the inline rename input border: change `border-primary` (already correct, will now be indigo)
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>FAB uses indigo idle color. Processing badge uses indigo tint. Evidence links are indigo. Outcome cards have hover shadows. Transcript speaker colors complement indigo. All page headings use font-heading. Tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification checkpoint</name>
  <files>N/A</files>
  <action>Human visual verification of the complete UI/UX overhaul. Run dev server with npm run dev, then verify all visual and functional changes in the browser.</action>
  <what-built>Complete UI/UX overhaul: indigo theme, sidebar navigation, Space Grotesk + Inter typography, dashboard home with stats, Recording Hub with search/filter, polished outcome cards and transcript bubbles.</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 -- verify dashboard shows stat cards (indigo accents), recent lists
    2. Click sidebar items: Dashboard, Recordings work; Tasks/Documents show as disabled
    3. Collapse sidebar via trigger button -- should shrink to icon rail
    4. Check "Allure" text in sidebar header uses distinctive heading font
    5. Navigate to Recordings page -- verify search bar filters by title, project dropdown works
    6. Open a recording detail page -- verify indigo tabs, indigo evidence links, speaker colors complement indigo
    7. Click FAB -- verify indigo when idle, red pulse when recording
    8. Open DevTools > Computed Styles: headings should show "Space Grotesk", body should show "Inter"
    9. Verify no visual regressions: tables render, badges display, toasts work
  </how-to-verify>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>User has visually verified all UI/UX changes and approved the overhaul.</done>
  <resume-signal>Type "approved" or describe specific visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm test` passes (no regressions)
- `npx next build` succeeds
- Recording Hub search filters by title in real-time
- All components use indigo theme consistently
- Headings use Space Grotesk throughout
- Visual checkpoint confirms cohesive appearance
</verification>

<success_criteria>
- Recording Hub: search bar filters recordings, project dropdown filters by project
- All status badges, confidence indicators, evidence links use indigo/semantic colors
- Transcript bubbles use indigo-complementary palette
- Recording FAB: indigo idle, red recording
- All page/section headings in Space Grotesk
- Human visual verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ui-ux-overhaul-modern-saas-dashboard/02.1-03-SUMMARY.md`
</output>

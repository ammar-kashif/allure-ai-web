---
phase: 04-document-generation-and-demo-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/db/documents.test.ts
  - src/app/api/documents/__tests__/route.test.ts
  - backend/tests/test_document_generation.py
  - src/components/document/mermaid-diagram.test.tsx
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "Test stubs exist for all document generation features before implementation begins"
    - "Running npm test and pytest includes document test files without errors"
  artifacts:
    - path: "src/lib/db/documents.test.ts"
      provides: "Unit test stubs for document CRUD operations"
      contains: "describe.*documents"
    - path: "src/app/api/documents/__tests__/route.test.ts"
      provides: "Integration test stubs for document API routes"
      contains: "describe.*documents.*api"
    - path: "backend/tests/test_document_generation.py"
      provides: "Test stubs for backend Mermaid/PRD generation"
      contains: "test_generate"
    - path: "src/components/document/mermaid-diagram.test.tsx"
      provides: "Unit test stubs for Mermaid parse validation"
      contains: "describe.*mermaid"
  key_links: []
---

<objective>
Create Wave 0 test stub files for Phase 4 document generation features, establishing the Nyquist test scaffolds before any implementation begins.

Purpose: Satisfies the Nyquist rule -- every task verify must have an automated test. These stubs define expected behavior contracts that Plans 01 and 02 will make pass.
Output: 4 test stub files with pending/skipped tests covering DOC-01 (CRUD + API), DOC-02 (generation), and DOC-03 (Mermaid rendering).
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-document-generation-and-demo-polish/04-CONTEXT.md
@.planning/phases/04-document-generation-and-demo-polish/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 4 test stub files for document generation</name>
  <files>
    src/lib/db/documents.test.ts,
    src/app/api/documents/__tests__/route.test.ts,
    backend/tests/test_document_generation.py,
    src/components/document/mermaid-diagram.test.tsx
  </files>
  <action>
    Create 4 test stub files with `describe` blocks and `it.todo()` (vitest) or `@pytest.mark.skip` (pytest) placeholders. Tests define the behavioral contracts -- they will be fleshed out and made to pass during Plans 01 and 02.

    **1. src/lib/db/documents.test.ts (DOC-01 CRUD):**
    ```typescript
    import { describe, it, expect } from "vitest"

    describe("documents db module", () => {
      it.todo("createDocument inserts a document and returns it with generated id")
      it.todo("getDocument returns a document by id")
      it.todo("getDocument returns null for non-existent id")
      it.todo("listDocuments returns all documents ordered by created_at DESC")
      it.todo("listDocuments filters by type when type param provided")
      it.todo("countDocuments returns the total number of documents")
    })
    ```

    **2. src/app/api/documents/__tests__/route.test.ts (DOC-01 API):**
    Create the `__tests__` directory first.
    ```typescript
    import { describe, it, expect } from "vitest"

    describe("documents API routes", () => {
      it.todo("GET /api/documents returns list of documents")
      it.todo("GET /api/documents?type=prd filters by type")
      it.todo("GET /api/documents/[id] returns a single document")
      it.todo("GET /api/documents/[id] returns 404 for non-existent id")
      it.todo("POST /api/recordings/[id]/generate-prd triggers generation and returns 201")
      it.todo("POST /api/recordings/[id]/generate-diagram validates type field")
    })
    ```

    **3. backend/tests/test_document_generation.py (DOC-02 generation):**
    ```python
    import pytest

    class TestPRDGeneration:
        @pytest.mark.skip(reason="Wave 0 stub - Plan 01 implements")
        def test_generate_prd_returns_markdown_string(self):
            pass

        @pytest.mark.skip(reason="Wave 0 stub - Plan 01 implements")
        def test_generate_prd_includes_outcomes_in_prompt(self):
            pass

        @pytest.mark.skip(reason="Wave 0 stub - Plan 01 implements")
        def test_format_outcomes_for_generation(self):
            pass

    class TestDiagramGeneration:
        @pytest.mark.skip(reason="Wave 0 stub - Plan 01 implements")
        def test_generate_diagram_user_flow_returns_mermaid(self):
            pass

        @pytest.mark.skip(reason="Wave 0 stub - Plan 01 implements")
        def test_generate_diagram_erd_returns_mermaid(self):
            pass

        @pytest.mark.skip(reason="Wave 0 stub - Plan 01 implements")
        def test_generate_diagram_invalid_type_raises(self):
            pass
    ```

    **4. src/components/document/mermaid-diagram.test.tsx (DOC-03 Mermaid):**
    Create the `src/components/document/` directory first.
    ```typescript
    import { describe, it, expect } from "vitest"

    describe("MermaidDiagram component", () => {
      it.todo("renders valid Mermaid code as SVG")
      it.todo("shows error fallback for invalid Mermaid syntax")
      it.todo("displays raw code in fallback when parse fails")
    })
    ```

    All stubs use `.todo()` or `@pytest.mark.skip` so they are recognized by the test runner but do not fail.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npm test 2>&1 | tail -20 && cd backend && python -m pytest tests/test_document_generation.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>All 4 test stub files exist, are discovered by their respective test runners, and do not cause failures (all skipped/todo).</done>
</task>

</tasks>

<verification>
- `npm test` discovers and reports the 3 vitest stub files as todo
- `cd backend && python -m pytest tests/test_document_generation.py` discovers and reports 6 skipped tests
- No test failures introduced
</verification>

<success_criteria>
- 4 test stub files created at the exact paths listed in VALIDATION.md Wave 0 Requirements
- Test runners discover all stubs without errors
- Stubs define behavioral contracts matching DOC-01, DOC-02, DOC-03 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/04-document-generation-and-demo-polish/04-00-SUMMARY.md`
</output>

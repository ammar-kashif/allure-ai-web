---
phase: 04-document-generation-and-demo-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(dashboard)/documents/page.tsx
  - src/app/(dashboard)/documents/[id]/page.tsx
  - src/components/document/mermaid-diagram.tsx
  - src/components/document/document-type-badge.tsx
  - src/app/(dashboard)/recordings/[id]/page.tsx
autonomous: false
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "User can see a list of generated documents with type badges and source recording"
    - "User can click a document to view its full content on a detail page"
    - "User can generate a PRD from the recording detail page"
    - "User can generate a Mermaid diagram (User Flow or ERD) from the recording detail page"
    - "Generated Mermaid diagrams render as inline SVG without syntax errors"
    - "Invalid Mermaid syntax shows a fallback with raw code"
  artifacts:
    - path: "src/app/(dashboard)/documents/page.tsx"
      provides: "Documents list page with type filter tabs"
      min_lines: 60
    - path: "src/app/(dashboard)/documents/[id]/page.tsx"
      provides: "Document detail page with PRD or diagram rendering"
      min_lines: 50
    - path: "src/components/document/mermaid-diagram.tsx"
      provides: "Client-side Mermaid renderer with error fallback"
      min_lines: 30
    - path: "src/components/document/document-type-badge.tsx"
      provides: "Type badge component (PRD/User Flow/ERD)"
      min_lines: 15
  key_links:
    - from: "src/app/(dashboard)/documents/page.tsx"
      to: "src/hooks/use-documents.ts"
      via: "useDocuments hook"
      pattern: "useDocuments"
    - from: "src/app/(dashboard)/documents/[id]/page.tsx"
      to: "src/components/document/mermaid-diagram.tsx"
      via: "MermaidDiagram component for user_flow/erd types"
      pattern: "MermaidDiagram"
    - from: "src/app/(dashboard)/recordings/[id]/page.tsx"
      to: "src/hooks/use-documents.ts"
      via: "useGeneratePrd and useGenerateDiagram mutations"
      pattern: "useGenerate(Prd|Diagram)"
    - from: "src/components/document/mermaid-diagram.tsx"
      to: "mermaid"
      via: "dynamic import and mermaid.render()"
      pattern: "import.*mermaid.*render"
---

<objective>
Build the UI layer for document generation: documents list page, document detail page with Mermaid rendering, generate buttons on recording detail, and type badge component.

Purpose: Completes the user-facing document generation feature. Users can generate PRDs and diagrams from recordings, view them in a documents list, and see rendered Mermaid diagrams.
Output: Functional /documents and /documents/[id] pages, generate buttons on recording detail, Mermaid renderer with error fallback.
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-document-generation-and-demo-polish/04-CONTEXT.md
@.planning/phases/04-document-generation-and-demo-polish/04-RESEARCH.md
@.planning/phases/04-document-generation-and-demo-polish/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/db/documents.ts:
```typescript
export interface Document {
  id: string
  title: string
  type: "prd" | "user_flow" | "erd"
  content: string
  sourceRecordingId: string
  createdAt: string
}

export function createDocument(data: {...}): Document
export function getDocument(id: string): Document | null
export function listDocuments(filters?: { type?: string | null }): Document[]
export function countDocuments(): number
```

From src/hooks/use-documents.ts:
```typescript
export function useDocuments(type?: string): UseQueryResult<Document[]>
export function useDocument(id: string): UseQueryResult<Document>
export function useGeneratePrd(recordingId: string): UseMutationResult<Document>
export function useGenerateDiagram(recordingId: string): UseMutationResult<Document>
```

From src/app/(dashboard)/recordings/[id]/page.tsx (existing structure):
```typescript
// Tabs: Info (0), Transcript (1), Outcomes (2)
// Status-dependent content: unassigned, processing, error, ready
// "ready" status shows Tabs component
// Pattern: buttons in the Info tab or header area
```

From src/components/ui/table.tsx, tabs.tsx, badge.tsx, skeleton.tsx:
```
// shadcn/ui components already available
// Tab pattern: All / filtered tabs (see Recording Hub, Task list)
// Badge: variant prop for styling
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documents list page, detail page, and Mermaid renderer</name>
  <files>
    src/app/(dashboard)/documents/page.tsx,
    src/app/(dashboard)/documents/[id]/page.tsx,
    src/components/document/mermaid-diagram.tsx,
    src/components/document/document-type-badge.tsx
  </files>
  <action>
    **Install mermaid:**
    Run `npm install mermaid` before creating components.

    **DocumentTypeBadge (document-type-badge.tsx):**
    Create a simple component that renders a Badge with color-coded variants:
    - PRD: indigo badge (bg-primary/10 text-primary)
    - User Flow: teal badge (bg-teal-500/10 text-teal-600)
    - ERD: violet badge (bg-violet-500/10 text-violet-600)
    Props: `{ type: "prd" | "user_flow" | "erd" }`. Display labels: "PRD", "User Flow", "ERD".

    **MermaidDiagram (mermaid-diagram.tsx):**
    Create `"use client"` component following RESEARCH.md Pattern 3 exactly:
    - Props: `{ code: string }`
    - useEffect dynamically imports mermaid: `const mermaid = (await import("mermaid")).default`
    - Initialize with `mermaid.initialize({ startOnLoad: false, theme: "neutral" })`
    - Validate with `await mermaid.parse(code)` before rendering
    - Render with `const { svg } = await mermaid.render(\`mermaid-${crypto.randomUUID()}\`, code)`
    - Set SVG via `containerRef.current.innerHTML = svg`
    - On error: show fallback UI with destructive border, error message, and raw code in `<pre>` block
    - Use `cancelled` flag in cleanup to prevent state updates after unmount
    - Container div: `className="flex justify-center p-4"`

    **Documents list page (documents/page.tsx):**
    Create `"use client"` page with:
    - Page heading "Documents" with font-heading styling (match Tasks/Recordings pages)
    - Tab bar: All / PRDs / Diagrams (using Tabs component, same pattern as Recording Hub and Task list)
    - "All" shows all documents, "PRDs" filters type=prd, "Diagrams" filters type in (user_flow, erd) -- for Diagrams tab, fetch all and filter client-side since the API supports single type filter
    - Table with columns: Title, Type (DocumentTypeBadge), Source Recording (show recording title or ID), Date Generated (formatted)
    - Each row is a Link to `/documents/${doc.id}`
    - Empty state: "No documents generated yet. Generate a PRD or diagram from a recording."
    - Loading state: Skeleton placeholders
    - Use `useDocuments()` hook for data fetching
    - For source recording display, fetch recording name if available or show sourceRecordingId as fallback

    **Document detail page (documents/[id]/page.tsx):**
    Create `"use client"` page with:
    - Back button linking to /documents (same BackButton pattern as recording detail)
    - Document title as heading with DocumentTypeBadge next to it
    - Creation date in muted text below heading
    - Content area:
      - If type is "prd": render content as styled prose. Use `whitespace-pre-wrap` CSS with the content in a `div`. Apply `prose`-like styling: `text-base leading-relaxed` in a card container. The LLM output uses markdown-style formatting (headers with **, bullets with -) which will be readable with pre-wrap. If the content uses markdown headers (lines starting with ## or **), split by lines and style headers with font-heading font-semibold and larger text.
      - If type is "user_flow" or "erd": render using `<MermaidDiagram code={document.content} />` component
    - 404 state if document not found
    - Loading skeleton
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Documents list page at /documents shows table with type badges and filter tabs. Document detail page at /documents/[id] renders PRD content or Mermaid diagram. MermaidDiagram component validates syntax and shows error fallback on invalid code.</done>
</task>

<task type="auto">
  <name>Task 2: Generate buttons on recording detail page</name>
  <files>src/app/(dashboard)/recordings/[id]/page.tsx</files>
  <action>
    Add "Generate PRD" and "Generate Diagram" buttons to the recording detail page, visible only when status is "ready".

    **Button placement:** Add buttons in the header area, after the status badge and dropdown menu, on the same line. Use a flex container with gap-2. Buttons should be variant="outline" size="sm" to not dominate the header.

    **Generate PRD button:**
    - Icon: FileText from lucide-react
    - Label: "Generate PRD"
    - On click: call `generatePrd.mutate()` from `useGeneratePrd(id)` hook
    - While generating (mutation isPending): show Loader2 spinner, disable button, label "Generating..."
    - On success: navigate to `/documents/${result.id}` using router.push so user sees the generated PRD immediately

    **Generate Diagram button:**
    - Icon: GitBranch or Share2 from lucide-react (use Share2 for flow diagram association)
    - This should be a dropdown button (DropdownMenu) with two options: "User Flow" and "ERD"
    - On selecting an option: call `generateDiagram.mutate({ type: selectedType })` from `useGenerateDiagram(id)` hook
    - While generating: show Loader2 spinner on the trigger button, disable it, label "Generating..."
    - On success: navigate to `/documents/${result.id}`

    **Import additions:**
    - Add `FileText, GitBranch` (or `Share2`) to lucide-react imports
    - Import `useGeneratePrd, useGenerateDiagram` from `@/hooks/use-documents`
    - Import `useRouter` is already imported

    **Guard:** Only show generate buttons when `recording.status === "ready"` -- they should appear alongside the Tabs, not in the unassigned/processing/error states. Place them between the header info and the Tabs component, in a flex row with gap-2.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Recording detail page shows "Generate PRD" button and "Generate Diagram" dropdown (User Flow / ERD options) when status is ready. Clicking generates via LLM and navigates to the new document page on success.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete document generation flow</name>
  <action>Human verifies the end-to-end document generation flow works correctly.</action>
  <what-built>Complete document generation flow: generate PRD and Mermaid diagrams from recording detail, view in documents list, render diagrams as SVG</what-built>
  <how-to-verify>
    1. Start the app: `npm run dev` (frontend) and `cd backend && python main.py` (backend)
    2. Navigate to a recording that has status "ready" with extracted outcomes
    3. Click "Generate PRD" button -- should show loading state, then navigate to the generated PRD document
    4. Verify PRD document shows formatted prose content with sections (Overview, Decisions, etc.)
    5. Go back to the same recording, click "Generate Diagram" > "User Flow"
    6. Verify the generated diagram renders as an inline SVG flowchart
    7. Navigate to /documents -- verify both documents appear in the table with correct type badges
    8. Check filter tabs: PRDs tab shows only the PRD, Diagrams tab shows only the diagram
    9. Navigate to Dashboard -- verify 4th stat card "Documents Generated" shows correct count
    10. Verify sidebar "Documents" link is active and navigates to /documents
  </how-to-verify>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>User has verified the complete document generation pipeline works end-to-end</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Documents list page renders at /documents
- Document detail page renders at /documents/[id]
- Mermaid diagrams render as SVG (or show error fallback for invalid syntax)
- Generate buttons appear on recording detail page when status is ready
- Full flow: generate -> store -> list -> view works end-to-end
</verification>

<success_criteria>
- /documents page shows document table with type filter tabs (All/PRDs/Diagrams)
- /documents/[id] renders PRD content or Mermaid diagram
- MermaidDiagram component uses mermaid.parse() for validation and shows fallback on error (DOC-03)
- Recording detail page has Generate PRD button and Generate Diagram dropdown
- Generation triggers LLM via backend, stores in SQLite, navigates to result
- User verifies the complete flow works
</success_criteria>

<output>
After completion, create `.planning/phases/04-document-generation-and-demo-polish/04-02-SUMMARY.md`
</output>

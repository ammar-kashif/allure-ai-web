---
phase: 02-ai-extraction-and-promotion
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/app/(dashboard)/recordings/[id]/page.tsx
  - src/components/outcome/outcomes-tab.tsx
  - src/components/outcome/outcome-card.tsx
  - src/components/outcome/outcome-section.tsx
  - src/components/outcome/summary-banner.tsx
  - src/components/transcript/transcript-view.tsx
  - src/components/outcome/__tests__/outcomes-tab.test.tsx
  - src/components/outcome/__tests__/outcome-card.test.tsx
autonomous: false
requirements: [EXT-03, EXT-04, EXT-05, EXT-06, EXT-07]

must_haves:
  truths:
    - "Recording detail page has three tabs: Info, Transcript, Outcomes"
    - "Outcomes tab shows summary banner with total count, type breakdown, and review count"
    - "Outcomes are grouped in collapsible sections by type (Decisions, Action Items, Requirements, Blockers) with count in header"
    - "Each outcome card shows type icon, bold title, detail excerpt, confidence score with color, evidence link, and promote button (if applicable)"
    - "Items below 0.80 confidence have amber left border, amber badge, and 'Needs review' label"
    - "Clicking evidence link switches to Transcript tab and highlights the utterance yellow for ~3 seconds"
    - "Clicking Promote on an action item creates a task with backlink, shows toast, replaces button with 'View Task'"
    - "Clicking Promote on a requirement creates a requirement record with backlink"
    - "Decisions and Blockers have no Promote button"
    - "While extraction is processing, Outcomes tab shows skeleton loading state"
  artifacts:
    - path: "src/components/outcome/outcomes-tab.tsx"
      provides: "Tab container with summary banner, grouped sections, loading/error states"
      min_lines: 50
    - path: "src/components/outcome/outcome-card.tsx"
      provides: "Individual outcome card with confidence coloring, evidence link, promote button"
      min_lines: 40
    - path: "src/components/outcome/outcome-section.tsx"
      provides: "Collapsible section per outcome type with count in header"
      min_lines: 20
    - path: "src/components/outcome/summary-banner.tsx"
      provides: "Stats banner: total outcomes, type breakdown, items needing review"
      min_lines: 15
    - path: "src/app/(dashboard)/recordings/[id]/page.tsx"
      provides: "Recording detail page with tabs (Info, Transcript, Outcomes)"
      contains: "TabsTrigger"
  key_links:
    - from: "src/components/outcome/outcome-card.tsx"
      to: "src/stores/evidence-highlight.ts"
      via: "setHighlight on evidence link click"
      pattern: "useEvidenceHighlight"
    - from: "src/components/outcome/outcome-card.tsx"
      to: "src/hooks/use-outcomes.ts"
      via: "usePromoteOutcome mutation on promote click"
      pattern: "usePromoteOutcome"
    - from: "src/components/transcript/transcript-view.tsx"
      to: "src/stores/evidence-highlight.ts"
      via: "reads highlightUtteranceIndex, scrolls to element, applies yellow highlight CSS"
      pattern: "useEvidenceHighlight"
    - from: "src/app/(dashboard)/recordings/[id]/page.tsx"
      to: "src/stores/evidence-highlight.ts"
      via: "activeTab controls which tab is shown"
      pattern: "activeTab"
---

<objective>
Build the Outcomes tab UI on the recording detail page: tabbed layout, summary banner, grouped outcome cards with confidence coloring, evidence cross-navigation to transcript with yellow highlight, one-click promotion with toast feedback, and loading/error states.

Purpose: This is the user-facing AI extraction experience -- the "wow factor" for the FYP demo. Professors see structured outcomes with evidence links at a glance.
Output: Complete Outcomes tab with all interactive behaviors, plus component tests.
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-extraction-and-promotion/02-CONTEXT.md
@.planning/phases/02-ai-extraction-and-promotion/02-RESEARCH.md
@.planning/phases/02-ai-extraction-and-promotion/02-01-SUMMARY.md
@.planning/phases/02-ai-extraction-and-promotion/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 02-02: Types and hooks this plan consumes -->

From src/types/outcome.ts:
```typescript
export type OutcomeType = 'decision' | 'action_item' | 'requirement' | 'blocker';
export type ExtractionStatus = 'none' | 'pending' | 'processing' | 'completed' | 'failed';
export interface EvidenceRef { segmentIndex: number; speaker: string; timestamp: number; textSnippet?: string; }
export interface Outcome { id: string; type: OutcomeType; title: string; detail: string; confidence: number; evidenceRefs: EvidenceRef[]; promoted: boolean; promotedId: string | null; }
export interface OutcomesResponse { jobId: string; extractionStatus: ExtractionStatus; outcomes: Outcome[]; }
```

From src/hooks/use-outcomes.ts:
```typescript
export function useOutcomes(recordingId: string, enabled: boolean): UseQueryResult<OutcomesResponse>;
export function useExtractionStatus(recordingId: string, enabled: boolean): UseQueryResult<...>;
export function usePromoteOutcome(): UseMutationResult<PromoteResponse, Error, { outcomeId: string; recordingId: string; outcomeIndex: number }>;
```

From src/stores/evidence-highlight.ts:
```typescript
export const useEvidenceHighlight: StoreApi<{
  highlightUtteranceIndex: number | null;
  activeTab: string;
  setHighlight: (index: number) => void;
  clearHighlight: () => void;
  setActiveTab: (tab: string) => void;
}>;
```

From src/components/transcript/transcript-view.tsx:
```typescript
export function TranscriptView({ transcript }: { transcript: Transcript }): JSX.Element;
// Needs modification to accept highlight state
```

From src/types/recording.ts:
```typescript
export interface Utterance { id: string; speaker: string; text: string; startTime: number; endTime: number; }
export interface Transcript { id: string; recordingId: string; utterances: Utterance[]; }
```

Existing UI components (already installed):
- src/components/ui/tabs.tsx (shadcn Tabs)
- src/components/ui/badge.tsx (shadcn Badge)
- src/components/ui/skeleton.tsx (shadcn Skeleton)
- sonner for toast notifications
- lucide-react for icons: FileText, CheckSquare, AlertTriangle, Target, ChevronDown, ExternalLink, Check
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Outcome components and recording detail page with tabs</name>
  <files>
    src/components/outcome/summary-banner.tsx,
    src/components/outcome/outcome-section.tsx,
    src/components/outcome/outcome-card.tsx,
    src/components/outcome/outcomes-tab.tsx,
    src/app/(dashboard)/recordings/[id]/page.tsx,
    src/components/outcome/__tests__/outcomes-tab.test.tsx,
    src/components/outcome/__tests__/outcome-card.test.tsx
  </files>
  <behavior>
    - outcomes-tab.test.tsx:
      - Renders summary banner with correct counts when outcomes provided
      - Shows skeleton loading state when extractionStatus is "processing"
      - Shows error state when extractionStatus is "failed"
      - Groups outcomes by type into collapsible sections
      - Shows "No outcomes" message when outcomes array is empty and status is completed
    - outcome-card.test.tsx:
      - Renders title, detail, confidence score for an outcome
      - Shows green confidence badge for score >= 0.80
      - Shows amber confidence badge with "Needs review" for score < 0.80
      - Shows amber left border for low-confidence items
      - Shows Promote button for action_item type
      - Shows Promote button for requirement type
      - Does NOT show Promote button for decision type
      - Does NOT show Promote button for blocker type
      - Shows "View Task" link instead of Promote when outcome.promoted is true
  </behavior>
  <action>
    1. Create src/components/outcome/summary-banner.tsx:
       - Props: outcomes: Outcome[]
       - Display: total count, breakdown by type (icon + count for each), count with confidence < 0.80 as "Needs review: N"
       - Use Badge component for type counts, compact horizontal layout
       - Use lucide icons: FileText (decision), CheckSquare (action_item), Target (requirement), AlertTriangle (blocker)

    2. Create src/components/outcome/outcome-section.tsx:
       - Props: type: OutcomeType, outcomes: Outcome[], children (render prop or direct)
       - Collapsible section using shadcn Collapsible (check if exists, otherwise use simple state toggle with ChevronDown icon)
       - Header: type icon + type label + count badge, clickable to expand/collapse
       - Default expanded
       - Type labels: "Decisions", "Action Items", "Requirements", "Blockers"

    3. Create src/components/outcome/outcome-card.tsx:
       - Props: outcome: Outcome, recordingId: string, outcomeIndex: number
       - Layout: card with left border (green if confidence >= 0.80, amber if < 0.80)
       - Title: bold, 1 line
       - Detail: text-muted-foreground, max 2 lines with line-clamp
       - Confidence: numeric badge (e.g., "0.92") with bg-green-100 text-green-800 or bg-amber-100 text-amber-800
       - Low confidence: add "Needs review" label next to badge
       - Evidence link: Show primary evidence ref as "{mm:ss} -- {speaker}" with ExternalLink icon. If multiple refs, show "+{N} more" that expands to show all. On click: call useEvidenceHighlight().setHighlight(ref.segmentIndex).
       - Promote button: Show for action_item and requirement types only. Use Button variant="outline" size="sm". On click: call usePromoteOutcome().mutate(). During mutation: show loading spinner. On success: show toast via sonner ("Task created" or "Requirement created"), button replaced with checkmark + "View Task"/"View Requirement" text.
       - If outcome.promoted: Show Check icon + "Promoted" badge instead of button. No re-promote.

    4. Create src/components/outcome/outcomes-tab.tsx:
       - Props: recordingId: string
       - Uses useOutcomes(recordingId, true) and useExtractionStatus hooks
       - States:
         - extractionStatus "pending"/"processing": Show skeleton loading (3-4 skeleton cards)
         - extractionStatus "failed": Show error message with red border
         - extractionStatus "completed" with empty outcomes: Show "No outcomes extracted" message
         - extractionStatus "completed" with outcomes: Show SummaryBanner + OutcomeSections
       - Group outcomes by type using: outcomes.filter(o => o.type === type)
       - Only show sections that have outcomes (skip empty types)
       - Order: Decisions, Action Items, Requirements, Blockers

    5. Modify src/app/(dashboard)/recordings/[id]/page.tsx:
       - Import shadcn Tabs, TabsList, TabsTrigger, TabsContent from @/components/ui/tabs
       - Import OutcomesTab from @/components/outcome/outcomes-tab
       - Import useEvidenceHighlight store
       - When recording.status === "ready": Wrap content in Tabs component with value controlled by useEvidenceHighlight().activeTab and onValueChange calling setActiveTab
       - Three tabs: "Info" (recording metadata -- move the existing header info here), "Transcript" (existing TranscriptView), "Outcomes" (OutcomesTab component)
       - Default active tab: "transcript" (matches current behavior)
       - Keep existing status-based rendering (unassigned, processing, error) outside tabs -- tabs only appear for "ready" status

    6. Write component tests:
       - src/components/outcome/__tests__/outcomes-tab.test.tsx: Mock useOutcomes hook, render OutcomesTab with various states, verify correct rendering
       - src/components/outcome/__tests__/outcome-card.test.tsx: Render OutcomeCard with various outcome configs, verify confidence coloring, promote button visibility, promoted state

    Use happy-dom environment (already configured in vitest). Mock hooks with vi.mock. Use @testing-library/react for render/screen/fireEvent.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npx vitest run src/components/outcome/__tests__/ --reporter=verbose</automated>
  </verify>
  <done>
    - OutcomesTab renders grouped outcomes with summary banner
    - OutcomeCard shows confidence coloring (green >= 0.80, amber < 0.80)
    - Promote button only on action_items and requirements
    - Promoted items show checkmark badge instead of button
    - Loading skeleton shown during extraction
    - Recording detail page has three tabs when status is "ready"
    - Component tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Evidence cross-navigation and transcript highlight</name>
  <files>
    src/components/transcript/transcript-view.tsx,
    src/components/transcript/utterance-bubble.tsx
  </files>
  <action>
    1. Modify src/components/transcript/transcript-view.tsx:
       - Import useEvidenceHighlight store
       - Read highlightUtteranceIndex from store
       - Pass highlightIndex to each UtteranceBubble (compare utterance index with highlightUtteranceIndex)
       - When highlightUtteranceIndex changes (useEffect), scroll the highlighted element into view using Element.scrollIntoView({ behavior: 'smooth', block: 'center' })
       - Use a ref map (Map<number, HTMLDivElement>) or data-attribute to find the target element

    2. Modify src/components/transcript/utterance-bubble.tsx (or the wrapper div in transcript-view.tsx):
       - Accept highlighted?: boolean prop
       - When highlighted: apply yellow background (bg-yellow-200/60 or similar) with CSS transition
       - Use transition-colors duration-1000 so the highlight fades smoothly
       - The highlight auto-clears after 3 seconds (handled by the store's setTimeout in setHighlight)

    3. Verify the full flow works:
       - Evidence link click in OutcomeCard -> setHighlight(segmentIndex) -> activeTab switches to "transcript" -> TranscriptView scrolls to utterance -> yellow highlight appears -> fades after 3s

    Keep changes minimal to existing components -- add the highlight prop, add the scroll effect, nothing else changes.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Clicking evidence link in Outcomes tab switches to Transcript tab
    - Target utterance scrolls into view smoothly
    - Yellow highlight appears on the utterance and fades after ~3 seconds
    - Existing transcript rendering unchanged (no visual regression)
    - All tests pass, no type errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete extraction and promotion flow</name>
  <files>none</files>
  <action>
    Human verifies the complete AI extraction and promotion UI flow on the recording detail page.
    Both servers must be running: backend (uvicorn) and frontend (npm run dev).
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npx vitest run --reporter=verbose && cd backend && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Human approves the full extraction and promotion flow</done>
  <what-built>Complete AI extraction and promotion UI flow on the recording detail page</what-built>
  <how-to-verify>
    1. Start both servers: backend (`cd backend && uvicorn main:app --reload`) and frontend (`npm run dev`)
    2. Navigate to a recording that has completed transcription (status: "ready")
    3. Verify three tabs appear: Info, Transcript, Outcomes
    4. If extraction hasn't run yet, trigger it: `curl -X POST http://localhost:8000/recordings/{id}/extract`
    5. Watch the Outcomes tab -- it should show a loading skeleton while extracting
    6. Once extraction completes, verify:
       a. Summary banner shows total outcomes, type breakdown, and review count
       b. Outcomes are grouped in collapsible sections by type
       c. Each card shows title, detail, confidence score with color (green >= 0.80, amber < 0.80)
       d. Low-confidence items have amber left border and "Needs review" label
    7. Click an evidence link on any outcome card -- verify it switches to Transcript tab, scrolls to the utterance, and highlights it yellow (fades after ~3 seconds)
    8. Click "Promote" on an action item -- verify toast says "Task created", button changes to "Promoted" checkmark
    9. Click "Promote" on a requirement -- verify toast says "Requirement created"
    10. Verify Decisions and Blockers have NO promote button
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Full test suite (frontend + backend):
```bash
cd /Users/ammarkashif/Documents/Code/allure-ai && npx vitest run --reporter=verbose && cd backend && python -m pytest tests/ -x -q
```
All tests pass. Human verification of visual correctness and interaction flow.
</verification>

<success_criteria>
- Recording detail page has Info/Transcript/Outcomes tabs
- Outcomes display grouped by type with confidence coloring
- Low-confidence items visually distinct (amber border + badge + "Needs review")
- Evidence links cross-navigate to transcript with yellow highlight fade
- One-click promotion creates task/requirement with toast feedback
- Promoted items show checkmark, cannot be re-promoted
- Decisions and Blockers are informational only (no promote button)
- Human approves the full flow
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-extraction-and-promotion/02-03-SUMMARY.md`
</output>

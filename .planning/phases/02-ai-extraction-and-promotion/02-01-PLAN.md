---
phase: 02-ai-extraction-and-promotion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/extraction.py
  - backend/job_queue.py
  - backend/storage.py
  - backend/models.py
  - backend/main.py
  - backend/tests/test_extraction.py
  - backend/tests/test_api.py
autonomous: true
requirements: [EXT-01, EXT-02, EXT-07]
user_setup:
  - service: huggingface
    why: "Download Phi-4-mini-instruct GGUF model (~2.5GB)"
    env_vars: []
    dashboard_config:
      - task: "Download model file before running extraction"
        location: "Run: huggingface-cli download bartowski/microsoft_Phi-4-mini-instruct-GGUF --include 'microsoft_Phi-4-mini-instruct-Q4_K_M.gguf' --local-dir ./backend/models/"

must_haves:
  truths:
    - "After STT completes, extraction is automatically enqueued without user action"
    - "Extraction produces structured outcomes (decisions, action items, requirements, blockers) from transcript text"
    - "Each outcome includes title, detail, confidence score (0-1), and evidence references to transcript segments"
    - "Evidence references point to valid segment indices in the source transcript"
    - "GET /recordings/{id}/outcomes returns outcomes with extraction_status"
    - "Promoted tasks/requirements include backlink text: 'From: {recording} @ {timestamp} -- {speaker}'"
    - "POST /recordings/{id}/extract enforces one-shot extraction -- returns 409 if extraction_status is not 'none'"
  artifacts:
    - path: "backend/extraction.py"
      provides: "LLM extraction logic with Phi-4-mini via llama-cpp-python"
      exports: ["run_extraction", "EXTRACTION_SCHEMA"]
    - path: "backend/models.py"
      provides: "Outcome, EvidenceRef, OutcomesResponse Pydantic models"
      contains: "class Outcome"
    - path: "backend/job_queue.py"
      provides: "Extended job queue that chains extraction after STT"
      contains: "extract"
    - path: "backend/main.py"
      provides: "New endpoints: GET /outcomes, POST /extract, POST /promote"
      contains: "outcomes"
  key_links:
    - from: "backend/job_queue.py"
      to: "backend/extraction.py"
      via: "run_extraction call after STT completes"
      pattern: "run_extraction"
    - from: "backend/extraction.py"
      to: "llama_cpp.Llama"
      via: "model inference with JSON schema constraint"
      pattern: "create_chat_completion"
    - from: "backend/main.py"
      to: "backend/storage.py"
      via: "get/update job with outcomes and extraction_status"
      pattern: "get_job.*outcomes"
---

<objective>
Build the Python backend extraction pipeline: LLM extraction module using Phi-4-mini via llama-cpp-python, extend the job queue to auto-chain extraction after STT, add outcome/promote/extract endpoints, and write backend tests with mocked LLM.

Purpose: This is the core AI differentiation -- turning raw transcripts into structured, confidence-scored outcomes with evidence links.
Output: Working backend extraction pipeline with endpoints, tested with mocked LLM.
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-extraction-and-promotion/02-CONTEXT.md
@.planning/phases/02-ai-extraction-and-promotion/02-RESEARCH.md
@.planning/phases/02-ai-extraction-and-promotion/02-VALIDATION.md

<interfaces>
<!-- Existing backend contracts the executor needs -->

From backend/storage.py:
```python
jobs: dict[str, dict[str, Any]] = {}

def create_job(job_id: str, file_path: str, original_filename: str) -> dict[str, Any]:
def update_job(job_id: str, **kwargs: Any) -> dict[str, Any]:
def get_job(job_id: str) -> dict[str, Any] | None:
```

From backend/models.py:
```python
class UploadResponse(BaseModel): id: str
class StatusResponse(BaseModel): status: Literal["pending", "processing", "completed", "failed"]
class TranscriptSegment(BaseModel): start: float, end: float, text: str, speaker: str, confidence: float
class TranscriptResponse(BaseModel): id: str, duration: float, language: str, speakers: list[SpeakerStats], segments: list[TranscriptSegment]
```

From backend/job_queue.py:
```python
job_queue: asyncio.Queue[str] = asyncio.Queue()

async def process_worker(app_state: object) -> None:
    # Currently processes STT only, needs extension for extraction chaining
```

From backend/main.py:
```python
# Lifespan loads: app.state.transcriber (Moonshine), app.state.diarizer (SpeechBrain)
# Endpoints: POST /recordings, GET /recordings/{job_id}/status, GET /recordings/{job_id}/transcript
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extraction module, Pydantic models, and backend tests</name>
  <files>
    backend/extraction.py,
    backend/models.py,
    backend/tests/test_extraction.py
  </files>
  <behavior>
    - test_extraction_produces_outcomes: Given a mocked LLM that returns valid JSON, run_extraction returns a list of Outcome dicts with type, title, detail, confidence, evidence_refs
    - test_outcome_schema_validation: Each outcome in the response has all required fields and valid types (confidence 0-1, type in enum, evidence_refs is list)
    - test_invalid_segment_indices_filtered: Outcomes with segment_index beyond transcript length are filtered out
    - test_backlink_format: format_backlink() produces "From: {title} @ {mm:ss} -- {speaker}" string
    - test_format_transcript_for_prompt: Segments are formatted as "[index] timestamp speaker: text" lines
  </behavior>
  <action>
    1. Extend backend/models.py with new Pydantic models:
       - EvidenceRef(BaseModel): segment_index (int), speaker (str), timestamp (float), text_snippet (str = "")
       - Outcome(BaseModel): id (str), type (Literal["decision","action_item","requirement","blocker"]), title (str), detail (str), confidence (float, ge=0, le=1), evidence_refs (list[EvidenceRef]), promoted (bool = False), promoted_id (str | None = None)
       - OutcomesResponse(BaseModel): job_id (str), extraction_status (Literal["none","pending","processing","completed","failed"]), outcomes (list[Outcome] = [])
       - PromoteRequest(BaseModel): (empty body -- one-click promote)
       - PromoteResponse(BaseModel): id (str), type (Literal["task","requirement"]), backlink (str)

    2. Create backend/extraction.py:
       - SYSTEM_PROMPT constant: Instruct Phi-4-mini to extract decisions, action_items, requirements, blockers from transcript. Include rules about confidence scoring (below 0.80 for ambiguous items), evidence_refs referencing segment indices.
       - EXTRACTION_SCHEMA dict: JSON schema matching the Outcome structure (as shown in RESEARCH.md Pattern 2)
       - format_transcript_for_prompt(segments): Format as "[i] mm:ss Speaker: text" lines
       - format_backlink(recording_title, timestamp, speaker): Returns "From: {title} @ {m}:{ss} -- {speaker}"
       - run_extraction(job_id, app_state): Get job transcript segments, format prompt, call app_state.llm.create_chat_completion with response_format JSON schema constraint, temperature=0.1, max_tokens=4096. Parse response, validate segment indices (filter out-of-range refs), assign UUID ids to each outcome, return list of outcome dicts.

    3. Create backend/tests/test_extraction.py:
       - Mock the LLM (app_state.llm) to return a canned JSON response
       - Mock get_job to return a job with transcript segments
       - Test all behaviors listed above
       - Use pytest fixtures for mock app_state and sample transcript data

    IMPORTANT: Do NOT load the actual LLM in tests. Mock app_state.llm.create_chat_completion to return predefined JSON.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai/backend && python -m pytest tests/test_extraction.py -x -q</automated>
  </verify>
  <done>
    - extraction.py exists with run_extraction, format_transcript_for_prompt, format_backlink, EXTRACTION_SCHEMA
    - models.py extended with EvidenceRef, Outcome, OutcomesResponse, PromoteRequest, PromoteResponse
    - All test_extraction.py tests pass with mocked LLM
    - Invalid segment indices are filtered from evidence_refs
    - Backlink format matches "From: {title} @ {m:ss} -- {speaker}"
  </done>
</task>

<task type="auto">
  <name>Task 2: Job queue chaining, endpoints, and API tests</name>
  <files>
    backend/job_queue.py,
    backend/storage.py,
    backend/main.py,
    backend/tests/test_api.py
  </files>
  <action>
    1. Extend backend/storage.py:
       - Add extraction_status, extraction_error, and outcomes fields to the job dict in create_job (extraction_status="none", outcomes=[])
       - No structural changes needed -- update_job already accepts **kwargs

    2. Modify backend/job_queue.py to support chained extraction:
       - Change queue type from Queue[str] to Queue[tuple[str, str]] where tuple is (job_id, job_type)
       - Update process_worker to handle two job types: "stt" and "extract"
       - For "stt" jobs: process as before, but after completion auto-enqueue (job_id, "extract") and set extraction_status="pending"
       - For "extract" jobs: set extraction_status="processing", call run_extraction from extraction.py, store outcomes in job, set extraction_status="completed"
       - On extract failure: set extraction_status="failed", extraction_error=str(exc)
       - Update the put call in main.py's upload_recording to use (job_id, "stt") tuple
       - IMPORTANT: Import run_extraction inside the elif branch to avoid circular imports

    3. Extend backend/main.py with new endpoints:
       - GET /recordings/{job_id}/status: Extend StatusResponse to include extraction_status field. Return both status and extraction_status.
       - GET /recordings/{job_id}/outcomes: Return OutcomesResponse with job_id, extraction_status, and outcomes list. 404 if job not found. Return empty outcomes if extraction not completed.
       - POST /recordings/{job_id}/extract: Manually trigger extraction (for testing). Enforces one-shot extraction per user decision: only enqueue if BOTH status=="completed" (STT done) AND extraction_status=="none" (never extracted). If extraction_status is "pending", "processing", or "completed", return 409 Conflict with message "Extraction already triggered or completed for this recording". If status!="completed", return 400. On success, enqueues (job_id, "extract"), sets extraction_status="pending", returns 202.
       - POST /recordings/{job_id}/outcomes/{outcome_index}/promote: Promote an outcome by index. Validate: outcome exists, type is action_item or requirement, not already promoted. Generate UUID for promoted item. Set outcome.promoted=True, outcome.promoted_id=new_id. Build backlink string using format_backlink. Return PromoteResponse with id, type ("task" or "requirement"), backlink.

    4. Update StatusResponse in models.py: Add extraction_status: Literal["none","pending","processing","completed","failed"] = "none"

    5. Add/extend tests in backend/tests/test_api.py:
       - test_promote_action_item: Create a job with completed extraction and action_item outcome. POST promote. Verify 200, response has task id and backlink, outcome marked promoted.
       - test_promote_requirement: Same but with requirement type. Verify type="requirement".
       - test_promote_already_promoted: Attempt to promote same outcome twice. Second call returns 409.
       - test_promote_decision_rejected: Attempt to promote a decision. Returns 400.
       - test_outcomes_endpoint: GET /recordings/{id}/outcomes returns outcomes list.
       - test_status_includes_extraction: GET /recordings/{id}/status includes extraction_status field.
       - test_extract_one_shot_guard: Create a job with status="completed" and extraction_status="completed". POST /extract. Verify 409 Conflict. Also test with extraction_status="pending" and "processing" -- both return 409. Only extraction_status="none" allows 202.

    NOTE: For API tests, directly manipulate storage.jobs to set up test state (mock job with outcomes). Do NOT require the actual LLM.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai/backend && python -m pytest tests/test_api.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    - Job queue chains extraction automatically after STT completion
    - GET /recordings/{id}/status returns extraction_status alongside status
    - GET /recordings/{id}/outcomes returns OutcomesResponse
    - POST /recordings/{id}/extract returns 409 if extraction already triggered or completed (one-shot enforcement)
    - POST /recordings/{id}/outcomes/{index}/promote creates task/requirement with backlink
    - Promotion rejects decisions/blockers (400) and already-promoted items (409)
    - All backend tests pass (existing + new)
  </done>
</task>

</tasks>

<verification>
Run full backend test suite:
```bash
cd backend && python -m pytest tests/ -x -q --tb=short
```
All tests pass including existing transcription tests and new extraction/promotion tests.
</verification>

<success_criteria>
- Backend extraction pipeline complete: extraction.py module, extended job queue, new endpoints
- Extraction auto-chains after STT (no manual trigger needed in production flow)
- One-shot extraction enforced: POST /extract returns 409 if extraction_status is not "none"
- Outcomes include confidence scores and validated evidence references
- Promotion creates task/requirement records with correct backlink format
- All backend tests pass with mocked LLM (no actual model loading required for tests)
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-extraction-and-promotion/02-01-SUMMARY.md`
</output>

---
phase: 02-ai-extraction-and-promotion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_extraction.py
  - src/components/outcome/outcomes-tab.tsx
autonomous: true
gap_closure: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04
  - EXT-05
  - EXT-06
  - EXT-07

must_haves:
  truths:
    - "All 6 extraction unit tests pass (test_extraction_produces_outcomes, test_outcome_schema_validation, test_invalid_segment_indices_filtered, test_backlink_format, test_format_transcript_for_prompt, test_extraction_schema_has_required_fields)"
    - "No lint warnings in outcomes-tab.tsx"
  artifacts:
    - path: "backend/tests/test_extraction.py"
      provides: "Fixed extraction tests using storage SQLite API"
      contains: "storage.create_job"
    - path: "src/components/outcome/outcomes-tab.tsx"
      provides: "Clean map callback without unused variable"
  key_links:
    - from: "backend/tests/test_extraction.py"
      to: "backend/storage.py"
      via: "create_job + update_job API"
      pattern: "storage\\.create_job|storage\\.update_job"
---

<objective>
Fix broken extraction unit tests and clean up lint warning from Phase 2 verification.

Purpose: 3 of 6 extraction tests fail because the `sample_job` fixture in `test_extraction.py` directly assigns to `storage.jobs[job_id]`, which was removed during the SQLite storage refactor. The conftest.py already initializes a temp SQLite DB via `storage.init_db()` — the extraction test fixture just needs to use the `storage.create_job()` / `storage.update_job()` API instead.

Output: All 6 extraction tests green, lint warning resolved.
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-extraction-and-promotion/02-VERIFICATION.md

@backend/tests/test_extraction.py
@backend/tests/conftest.py
@backend/storage.py
@src/components/outcome/outcomes-tab.tsx

<interfaces>
<!-- From backend/storage.py — the API the fixture must use -->
def init_db(db_path: str | None = None) -> None
def create_job(job_id: str, file_path: str, original_filename: str) -> dict[str, Any]
def update_job(job_id: str, **kwargs: Any) -> dict[str, Any]
def get_job(job_id: str) -> dict[str, Any] | None

<!-- From backend/tests/conftest.py — autouse fixture already handling DB init -->
@pytest.fixture(autouse=True)
def reset_state(tmp_path):
    db_path = str(tmp_path / "test_allure.db")
    storage.init_db(db_path)
    # ... drains queue, creates uploads dir
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix extraction test fixtures to use SQLite storage API</name>
  <files>backend/tests/test_extraction.py</files>
  <action>
Replace the `sample_job` fixture (lines 111-131) that directly assigns to the removed `storage.jobs` dict. The conftest.py `reset_state` fixture (autouse=True) already calls `storage.init_db(db_path)` with a temp path before each test, so the DB is ready.

New `sample_job` fixture should:
1. Call `storage.create_job(job_id, "/fake/path.wav", "meeting.wav")` to insert the job row
2. Call `storage.update_job(job_id, status="completed", result={...segments...}, extraction_status="none", outcomes=[])` to set the transcript result that `run_extraction` reads via `get_job`
3. Return `job_id` as before

The `result` dict passed to `update_job` must match the shape that `run_extraction` expects: `{"id": job_id, "duration": 20.0, "language": "en", "speakers": [], "segments": SAMPLE_SEGMENTS}`.

Note: `update_job` stores `result` and `outcomes` as JSON strings internally (via `json.dumps`). The `get_job` function deserializes them back. This is transparent to the fixture — just pass the dict/list directly.

Do NOT change any test logic or assertions — only the fixture setup.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai/backend && python -m pytest tests/test_extraction.py -x -v</automated>
  </verify>
  <done>All 6 tests in test_extraction.py pass: test_extraction_produces_outcomes, test_outcome_schema_validation, test_invalid_segment_indices_filtered, test_backlink_format, test_format_transcript_for_prompt, test_extraction_schema_has_required_fields</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused variable in outcomes-tab map callback</name>
  <files>src/components/outcome/outcomes-tab.tsx</files>
  <action>
On line 92, change `{typeOutcomes.map((outcome, i) => (` to `{typeOutcomes.map((outcome) => (` — remove the unused `i` parameter from the map callback. The `key` prop uses `outcome.id`, not `i`, so the index is not needed.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npx vitest run src/components/outcome/__tests__/outcomes-tab.test.tsx --reporter=verbose</automated>
  </verify>
  <done>No unused variable warning for outcomes-tab.tsx; existing 5/5 outcomes-tab tests still pass</done>
</task>

</tasks>

<verification>
Run full backend test suite and frontend outcome tests to confirm no regressions:

```bash
cd backend && python -m pytest tests/ -x -v
cd .. && npx vitest run src/components/outcome/ --reporter=verbose
```

All extraction tests pass (6/6), all outcome component tests pass (14/14).
</verification>

<success_criteria>
- All 6 backend extraction tests pass (previously 3 were broken)
- All 22 backend API tests still pass (no regressions)
- All 14 frontend outcome component tests still pass
- No lint warnings in outcomes-tab.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-extraction-and-promotion/02-04-SUMMARY.md`
</output>

---
phase: 08-context-aware-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/storage.py
  - backend/document_generation.py
  - backend/tests/test_document_generation.py
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03]

must_haves:
  truths:
    - "PRD prompt instructs product-spec tone, not meeting-minutes tone"
    - "Diagram prompts use product-focused examples, not meeting-flow examples"
    - "build_document_context returns formatted text from attachments with truncation"
    - "Documents with empty extracted_text are filtered out"
    - "Multiple documents get equal token budget with per-doc truncation"
    - "generate_prd and generate_diagram accept optional document_context parameter"
  artifacts:
    - path: "backend/storage.py"
      provides: "get_attachments_with_text query"
      contains: "get_attachments_with_text"
    - path: "backend/document_generation.py"
      provides: "Overhauled prompts, build_document_context, updated generation functions"
      contains: "build_document_context"
    - path: "backend/tests/test_document_generation.py"
      provides: "Tests for context injection, truncation, prompt content, generation functions"
      min_lines: 80
  key_links:
    - from: "backend/document_generation.py"
      to: "backend/storage.py"
      via: "import get_attachments_with_text"
      pattern: "from storage import.*get_attachments_with_text"
    - from: "backend/document_generation.py"
      to: "PRD_SYSTEM_PROMPT"
      via: "prompt constant used in generate_prd"
      pattern: "PRD_SYSTEM_PROMPT"
---

<objective>
Overhaul all generation prompts to produce product-focused output, add document context injection plumbing with token budget truncation, and write comprehensive tests.

Purpose: This is the core implementation -- prompts determine output quality, and context injection enables document-grounded generation. Tests verify prompt content and truncation logic without needing a running LLM.
Output: Rewritten prompts, build_document_context function, get_attachments_with_text query, updated generation function signatures, passing test suite.
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-context-aware-generation/08-CONTEXT.md
@.planning/phases/08-context-aware-generation/08-RESEARCH.md

@backend/document_generation.py
@backend/storage.py
@backend/tests/test_document_generation.py

<interfaces>
<!-- Existing storage functions used by document_generation.py -->
From backend/storage.py:
```python
def get_job(job_id: str) -> Optional[dict[str, Any]]:
    """Return job or None."""

def list_attachments(recording_id: str) -> list[dict[str, Any]]:
    """Return attachment metadata (without extracted_text) for a recording."""
    # SELECT id, recording_id, filename, file_type, file_size, extraction_error, created_at

def get_attachment(attachment_id: str) -> Optional[dict[str, Any]]:
    """Return full attachment record including extracted_text, or None."""
```

From backend/document_generation.py (current signatures to update):
```python
def generate_prd(job_id: str, app_state: object) -> str:
def generate_diagram(job_id: str, app_state: object) -> tuple[str, str]:
def select_diagram_type(outcomes: list[dict[str, Any]], app_state: object) -> str:
def format_outcomes_for_generation(outcomes: list[dict[str, Any]]) -> str:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Storage query + context injection + tests</name>
  <files>backend/storage.py, backend/document_generation.py, backend/tests/test_document_generation.py</files>
  <behavior>
    - get_attachments_with_text(recording_id) returns list of dicts with id, filename, extracted_text
    - get_attachments_with_text returns empty list when no attachments exist
    - build_document_context returns empty string when no attachments
    - build_document_context returns formatted "## Reference Documents" section with filename headers
    - build_document_context filters out attachments with empty/whitespace-only extracted_text
    - build_document_context truncates each doc to equal share of max_chars budget (default 16000)
    - Truncated docs end with "\n[truncated]" and header includes truncation note
    - build_document_context with single doc gets full budget
    - build_document_context with 2 docs splits budget 50/50
  </behavior>
  <action>
1. Add `get_attachments_with_text(recording_id)` to `storage.py`:
   - Query: `SELECT id, filename, extracted_text FROM attachments WHERE recording_id = ? ORDER BY created_at`
   - Filter out rows where `extracted_text` is empty or whitespace-only (per Pitfall 3 in research)
   - Return list of dicts

2. Add to `document_generation.py`:
   - Token budget constants per user decision:
     ```python
     TOKEN_BUDGET = {"system_prompt": 500, "outcomes": 2000, "documents": 4000, "generation": 1500}
     MAX_DOCUMENT_CHARS = TOKEN_BUDGET["documents"] * 4  # 16000
     ```
   - `build_document_context(recording_id: str, max_chars: int = MAX_DOCUMENT_CHARS) -> str`:
     - Import and call `get_attachments_with_text(recording_id)`
     - Return "" if no attachments (after filtering empty text)
     - Split `max_chars` equally among documents
     - For each doc: truncate to per_doc_budget, append `\n[truncated]` if truncated
     - Format as `## Reference Documents\n(Note: ...truncated...)\n\n### filename\ntext`
     - Per user decision: outcomes BEFORE documents, documents are "reference/supplementary"

3. Write tests in `test_document_generation.py` (replace skip stubs):
   - Test `build_document_context` with no attachments returns ""
   - Test with single doc returns formatted section
   - Test with multiple docs splits budget equally
   - Test truncation adds "[truncated]" marker and note in header
   - Test empty extracted_text docs are filtered out
   - Test `format_outcomes_for_generation` produces numbered list
   - Mock `get_attachments_with_text` in tests (no DB needed)
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai/backend && python -m pytest tests/test_document_generation.py -x -q -k "context or format"</automated>
  </verify>
  <done>get_attachments_with_text query exists in storage.py, build_document_context function handles all truncation/filtering cases, tests pass for context injection logic</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul all prompts and update generation functions</name>
  <files>backend/document_generation.py, backend/tests/test_document_generation.py</files>
  <action>
1. Rewrite `PRD_SYSTEM_PROMPT` per user decisions:
   - Product spec document tone, NOT meeting minutes
   - Sections: Overview, Goals and Objectives, Functional Requirements, Non-Functional Requirements, Constraints, Open Questions
   - Guard: "Do NOT reference speakers, timestamps, or meeting logistics. Write as a standalone product spec."
   - "Synthesize information naturally -- do not cite sources"
   - Use markdown formatting

2. Rewrite `USERFLOW_SYSTEM_PROMPT`:
   - "You are a product architect" framing
   - Anti-pattern: "IMPORTANT: Diagram the PRODUCT or SYSTEM discussed, NOT the meeting itself."
   - Replace meeting-flow example (current: "User Records Meeting -> Transcription") with product-domain example (e.g., "Customer Places Order -> Validate Payment")
   - Use entity names from outcomes and reference documents
   - Keep syntax rules (flowchart TD, max 12 nodes, no special chars, output ONLY mermaid)

3. Rewrite `ERD_SYSTEM_PROMPT`:
   - Product/system entities, NOT meeting entities
   - Replace example (current: USER/RECORDING/OUTCOME/TASK) with product-domain example (e.g., USER/ORDER/PRODUCT/PAYMENT)
   - Anti-pattern instruction same as user_flow
   - Keep syntax rules (erDiagram, max 6 entities, etc.)

4. Update `DIAGRAM_TYPE_SELECTOR_PROMPT`:
   - Per user decision: also consider "whether a product flow or architecture diagram is more appropriate"
   - Keep "user_flow" and "erd" as the two options
   - Frame selection around the product discussed, not the meeting

5. Update `generate_prd(job_id, app_state, document_context="")`:
   - Add optional `document_context: str = ""` parameter
   - Append document_context to user message after outcomes if non-empty
   - Frame outcomes as "## Meeting Outcomes (Primary Input)" and documents as supplementary (per Pitfall 5)
   - Keep temperature=0.3, max_tokens=4096

6. Update `generate_diagram(job_id, app_state, document_context="")`:
   - Same pattern: optional `document_context` parameter
   - Append to user message if non-empty
   - Keep temperature=0.2, max_tokens=2048

7. Add tests for prompt content validation:
   - PRD prompt contains "standalone product spec" and NOT "Reference specific speakers"
   - PRD prompt contains all 6 required sections
   - USERFLOW prompt contains "PRODUCT or SYSTEM" anti-pattern instruction
   - USERFLOW example does NOT contain "Records Meeting" or "Transcription"
   - ERD prompt does NOT contain "RECORDING" or "OUTCOME" as example entities
   - Test generate_prd with document_context appends it to user message (mock LLM)
   - Test generate_diagram with document_context appends it to user message (mock LLM)
   - Test generate_prd without document_context works same as before (backward compat)
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai/backend && python -m pytest tests/test_document_generation.py -x -q</automated>
  </verify>
  <done>All 4 prompt constants rewritten with product-focused content and anti-pattern guards. generate_prd and generate_diagram accept optional document_context. All tests pass including prompt content validation and backward compatibility.</done>
</task>

</tasks>

<verification>
- All tests pass: `cd backend && python -m pytest tests/test_document_generation.py -x -q`
- PRD_SYSTEM_PROMPT does NOT contain "speakers and timestamps"
- USERFLOW_SYSTEM_PROMPT example does NOT show meeting flow
- build_document_context handles 0, 1, N documents with truncation
- generate_prd and generate_diagram have backward-compatible signatures (document_context defaults to "")
</verification>

<success_criteria>
- All prompt constants rewritten for product-focused output
- build_document_context function with truncation and filtering
- get_attachments_with_text storage query
- Generation functions accept optional document context
- Test suite covers prompt content, context injection, truncation, backward compatibility
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/08-context-aware-generation/08-01-SUMMARY.md`
</output>

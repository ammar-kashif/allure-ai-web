---
phase: 08-context-aware-generation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/main.py
  - backend/tests/test_document_generation.py
autonomous: false
requirements: [GEN-01, GEN-02, GEN-03]

must_haves:
  truths:
    - "PRD generation endpoint fetches attachments and passes document context to generate_prd"
    - "Diagram generation endpoint fetches attachments and passes document context to generate_diagram"
    - "Generation works identically when no documents are attached (backward compat)"
    - "Generated PRD reads as a product spec, not meeting minutes"
    - "Generated diagrams model the product discussed, not the meeting flow"
  artifacts:
    - path: "backend/main.py"
      provides: "Wired generation endpoints with document context injection"
      contains: "build_document_context"
    - path: "backend/tests/test_document_generation.py"
      provides: "Integration-level tests for endpoint wiring"
      min_lines: 120
  key_links:
    - from: "backend/main.py"
      to: "backend/document_generation.py"
      via: "import build_document_context"
      pattern: "from document_generation import.*build_document_context"
    - from: "backend/main.py generate_prd_endpoint"
      to: "backend/document_generation.py generate_prd"
      via: "passes doc_context parameter"
      pattern: "generate_prd.*doc_context"
    - from: "backend/main.py generate_diagram_endpoint"
      to: "backend/document_generation.py generate_diagram"
      via: "passes doc_context parameter"
      pattern: "generate_diagram.*doc_context"
---

<objective>
Wire generation endpoints to fetch document context and pass it to generation functions, then verify end-to-end with a human check.

Purpose: This completes the pipeline -- endpoints now fetch attachments, build context, and pass it downstream. The checkpoint confirms output quality with real or mock data.
Output: Fully wired endpoints, passing test suite, human-verified output quality.
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-context-aware-generation/08-CONTEXT.md
@.planning/phases/08-context-aware-generation/08-01-SUMMARY.md

@backend/main.py
@backend/document_generation.py

<interfaces>
<!-- From Plan 01 output -->
From backend/document_generation.py (updated signatures):
```python
def build_document_context(recording_id: str, max_chars: int = 16000) -> str:
    """Fetch attachment texts and format as context section with truncation."""

def generate_prd(job_id: str, app_state: object, document_context: str = "") -> str:
    """Generate a PRD document from a recording's outcomes via LLM."""

def generate_diagram(job_id: str, app_state: object, document_context: str = "") -> tuple[str, str]:
    """Generate a Mermaid diagram from a recording's outcomes via LLM."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire generation endpoints with document context</name>
  <files>backend/main.py, backend/tests/test_document_generation.py</files>
  <action>
1. Modify `generate_prd_endpoint` in `main.py`:
   - Import `build_document_context` from `document_generation` (add to existing lazy import)
   - Before calling `generate_prd`, call `doc_context = build_document_context(job_id)`
   - Pass `doc_context` to `generate_prd`: `generate_prd(job_id, app.state, doc_context)`
   - No changes to response format -- same API contract

2. Modify `generate_diagram_endpoint` in `main.py`:
   - Same pattern: `doc_context = build_document_context(job_id)`
   - Pass to `generate_diagram`: `generate_diagram(job_id, app.state, doc_context)`
   - No changes to response format

3. Add wiring tests in `test_document_generation.py`:
   - Test that generate_prd with non-empty document_context includes it in user message
   - Test that generate_diagram with non-empty document_context includes it in user message
   - Verify document context appears AFTER outcomes in the prompt (per user decision)

4. Run full test suite to verify nothing is broken:
   - `cd backend && python -m pytest tests/ -x -q`
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai/backend && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Both generation endpoints fetch document context and pass it to generation functions. Full test suite passes. API contracts unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify generation output quality</name>
  <files>none</files>
  <action>
Human verifies that the complete context-aware generation pipeline produces product-focused output.

Steps:
1. Start the backend: `cd backend && python main.py`
2. Start the frontend: `cd frontend && npm run dev`
3. Open the app at http://localhost:3000
4. Navigate to a recording that has attached documents
5. Click "Generate PRD" -- verify the output:
   - Reads as a product spec, NOT meeting minutes
   - Has sections: Overview, Goals, Functional/Non-Functional Requirements, Constraints, Open Questions
   - Does NOT reference speakers or timestamps
   - Incorporates terminology from attached documents (if any)
6. Click "Generate Diagram" -- verify the output:
   - Models the product/system discussed, NOT the meeting flow
   - Uses domain terminology, not generic "Record Meeting -> Transcribe" patterns
   - Renders correctly as Mermaid
7. Test without documents: navigate to a recording WITHOUT attachments, generate PRD and diagram -- should work identically to before (backward compatible)
  </action>
  <verify>Human visual and functional verification of generated output quality</verify>
  <done>PRD reads as product spec with correct sections. Diagrams model the product discussed. Generation works with and without documents. User types "approved".</done>
</task>

</tasks>

<verification>
- Full test suite passes: `cd backend && python -m pytest tests/ -x -q`
- Generation endpoints import and call build_document_context
- PRD output reads as product spec with correct section structure
- Diagram output models product, not meeting flow
- Generation works with and without attached documents
</verification>

<success_criteria>
- Both endpoints wired to fetch and inject document context
- Human verifies PRD output quality (product-focused, correct sections)
- Human verifies diagram output quality (product-focused, valid Mermaid)
- Backward compatibility confirmed (generation works without documents)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-context-aware-generation/08-02-SUMMARY.md`
</output>

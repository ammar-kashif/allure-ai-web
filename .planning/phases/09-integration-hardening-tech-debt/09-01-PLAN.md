---
phase: 09-integration-hardening-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models.py
  - backend/document_generation.py
  - backend/tests/test_models_speaker_stats.py
autonomous: true
requirements:
  - SPKR-01
  - SPKR-02
  - GEN-02

must_haves:
  truths:
    - "SpeakerStats Pydantic model accepts custom_label and role fields with backward-compatible defaults"
    - "No function-level imports exist in document_generation.py"
    - "Existing backend tests still pass after changes"
  artifacts:
    - path: "backend/models.py"
      provides: "SpeakerStats with custom_label and role fields"
      contains: "custom_label"
    - path: "backend/document_generation.py"
      provides: "Top-level import re, no function-level import"
    - path: "backend/tests/test_models_speaker_stats.py"
      provides: "Unit tests for SpeakerStats model validation"
  key_links:
    - from: "backend/models.py"
      to: "backend/transcription.py"
      via: "SpeakerStats fields match what identify_speakers_with_llm produces"
      pattern: "custom_label.*role"
---

<objective>
Fix SpeakerStats Pydantic model fragility and clean up function-level import in document_generation.py.

Purpose: Ensure SpeakerStats model carries custom_label and role through Pydantic validation (INT-01 from audit), and eliminate tech debt function-level import.
Output: Updated backend/models.py with 2 new fields, cleaned document_generation.py, new unit test file.
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-hardening-tech-debt/09-RESEARCH.md

<interfaces>
<!-- Current SpeakerStats model (backend/models.py:33-45) -->
```python
class SpeakerStats(BaseModel):
    """Per-speaker statistics."""
    label: str
    talk_time_pct: float
    utterance_count: int
    talk_time: float
    word_count: int
    wpm: float
    turns: int
    avg_turn_duration: float
    pauses: int
    avg_pause_duration: float
    # MISSING: custom_label and role
```

<!-- Backend already produces these fields (transcription.py:459-460) -->
```python
speaker["custom_label"] = info.get("name", "")
speaker["role"] = info.get("role", "Participant")
```

<!-- document_generation.py imports (top of file) -->
```python
from typing import Any
from storage import get_attachments_with_text, get_job
```
<!-- Function-level import at line 263 -->
```python
import re  # inside generate_diagram()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add custom_label and role to SpeakerStats model with tests</name>
  <files>backend/models.py, backend/tests/test_models_speaker_stats.py</files>
  <behavior>
    - Test 1: SpeakerStats with all fields including custom_label and role validates successfully
    - Test 2: SpeakerStats without custom_label/role uses defaults (custom_label="" and role="Participant") -- backward compat
    - Test 3: SpeakerStats with empty string custom_label and explicit role validates correctly
    - Test 4: TranscriptResponse containing speakers with custom_label/role validates correctly
  </behavior>
  <action>
    1. Create `backend/tests/test_models_speaker_stats.py` with the tests above. Write RED tests first that import SpeakerStats from models and assert custom_label/role fields exist.
    2. Run tests -- they MUST fail (fields don't exist yet).
    3. Add two fields to `SpeakerStats` in `backend/models.py` after `avg_pause_duration`:
       - `custom_label: str = ""`
       - `role: str = "Participant"`
    4. Run tests -- they MUST pass.

    IMPORTANT: Do NOT add response_model to any endpoint. Only update the model definition. Do NOT change any endpoint code.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai/backend && python -m pytest tests/test_models_speaker_stats.py -x -v</automated>
  </verify>
  <done>SpeakerStats model has custom_label (default "") and role (default "Participant") fields. All 4 tests pass. Adding response_model=TranscriptResponse to the transcript endpoint would not drop data.</done>
</task>

<task type="auto">
  <name>Task 2: Move function-level import re to top of document_generation.py</name>
  <files>backend/document_generation.py</files>
  <action>
    1. Add `import re` after the `from typing import Any` line at the top of `backend/document_generation.py`.
    2. Remove the `import re` on line 263 (inside `generate_diagram()` function body).
    3. Verify no other function-level imports exist in the file.
    4. Run existing document generation tests to confirm nothing breaks.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai/backend && python -m pytest tests/test_document_generation.py -x -v && python -c "import ast; tree=ast.parse(open('document_generation.py').read()); bad=[n for n in ast.walk(tree) if isinstance(n,(ast.Import,ast.ImportFrom)) and n.col_offset>0]; assert not bad, f'Function-level imports found: {bad}'"</automated>
  </verify>
  <done>import re is at top of document_generation.py. No function-level imports exist anywhere in the file. Existing tests pass.</done>
</task>

</tasks>

<verification>
cd /Users/ammarkashif/Documents/Code/allure-ai/backend && python -m pytest tests/ -x -q
</verification>

<success_criteria>
- SpeakerStats model includes custom_label (str, default "") and role (str, default "Participant")
- Backward compatibility: old data without those fields still deserializes correctly
- No function-level imports in document_generation.py
- Full backend test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-hardening-tech-debt/09-01-SUMMARY.md`
</output>


---
phase: 09-integration-hardening-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/recordings/[id]/documents/route.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - RUX-02
  - DOC-02

must_haves:
  truths:
    - "Documents uploaded from the post-recording dialog are forwarded for text extraction even when backendId is not immediately available"
    - "PLAY-02 is marked complete in REQUIREMENTS.md"
    - "All completed phase plan checkboxes are marked in ROADMAP.md"
  artifacts:
    - path: "src/app/api/recordings/[id]/documents/route.ts"
      provides: "Retry logic for backendId resolution before forwarding documents"
      contains: "attempt"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate checkbox state matching audit findings"
    - path: ".planning/ROADMAP.md"
      provides: "All completed plan checkboxes marked [x]"
  key_links:
    - from: "src/app/api/recordings/[id]/documents/route.ts"
      to: "lib/db/recordings (getRecording)"
      via: "Retry loop re-fetches recording to get backendId"
      pattern: "getRecording.*backendId"
---

<objective>
Fix document forwarding race condition and clean up documentation checkboxes.

Purpose: Ensure documents uploaded from the post-recording dialog are forwarded for text extraction even when backendId is not yet available (INT-02/FLOW-01 from audit), and synchronize REQUIREMENTS.md and ROADMAP.md checkboxes with audit findings.
Output: Updated documents route with retry logic, accurate documentation checkboxes.
</objective>

<execution_context>
@/Users/ammarkashif/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ammarkashif/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-integration-hardening-tech-debt/09-RESEARCH.md

<interfaces>
<!-- Current documents route (src/app/api/recordings/[id]/documents/route.ts) -->
```typescript
import { getRecording } from "@/lib/db/recordings"

// Line 40-41: backendId checked once, no retry
const recording = getRecording(recordingId)
const backendId = recording?.backendId

// Line 55: conditional forwarding -- skips silently if null
if (backendId) {
  // forwards to Python backend for text extraction
}
```

<!-- Race condition timing:
1. RecordingFAB fires uploadRecording.mutate (async)
2. Simultaneously opens PostRecordingDialog
3. POST /api/recordings sets backendId after backend responds
4. User fills dialog and clicks Save
5. POST /api/recordings/{id}/documents checks backendId -- may be null
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backendId retry logic to documents route</name>
  <files>src/app/api/recordings/[id]/documents/route.ts</files>
  <action>
    Modify the POST handler in `src/app/api/recordings/[id]/documents/route.ts`:

    1. After the initial `getRecording(recordingId)` call on line 40, if `backendId` is null/undefined, add a retry loop:
       - Up to 5 attempts, 1 second apart (5 seconds max wait)
       - Each attempt calls `getRecording(recordingId)` again and checks `backendId`
       - Break on first successful retrieval
    2. Move the `backendId` check and forwarding logic AFTER the file save loop. Currently, forwarding happens inside the per-file loop (line 55). Restructure so:
       a. First loop: save all files to disk (existing behavior)
       b. After saving: resolve backendId (with retry if needed)
       c. Second loop: forward saved files to backend for extraction (if backendId resolved)
    3. This restructuring ensures ALL files are saved to disk first (graceful degradation preserved), then forwarding is attempted with the best-available backendId.

    The retry pattern per research:
    ```typescript
    let backendId = recording?.backendId
    if (!backendId) {
      for (let attempt = 0; attempt < 5; attempt++) {
        await new Promise(resolve => setTimeout(resolve, 1000))
        const freshRecording = getRecording(recordingId)
        if (freshRecording?.backendId) {
          backendId = freshRecording.backendId
          break
        }
      }
    }
    ```

    IMPORTANT: Do NOT introduce background job infrastructure, queues, or external dependencies. A simple retry loop is sufficient per research. Cap at 5 retries (5 seconds max). Preserve existing graceful degradation -- if retries fail, files are still saved to disk.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Documents route retries up to 5 times (1s apart) to resolve backendId before forwarding. Files are always saved to disk first. Forwarding happens after all files saved. Existing graceful degradation preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize REQUIREMENTS.md and ROADMAP.md checkboxes with audit</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    1. In `.planning/REQUIREMENTS.md`:
       - Verify PLAY-02 is marked `[x]` (audit confirmed SATISFIED but checkbox was `[ ]`). Looking at current file, PLAY-02 is already `[x]` -- confirm and leave as-is if correct.

    2. In `.planning/ROADMAP.md`:
       - Mark ALL Phase 5 plan checkboxes as `[x]`:
         - `[x] 05-01-PLAN.md`
         - `[x] 05-02-PLAN.md`
         - `[x] 05-03-PLAN.md`
         - `[x] 05-04-PLAN.md`
       - Mark ALL Phase 6 plan checkboxes as `[x]`:
         - `[x] 06-01-PLAN.md`
         - `[x] 06-02-PLAN.md`
         - `[x] 06-03-PLAN.md`
       - Verify Phase 7 and 8 checkboxes are already `[x]` (they appear correct in current file).

    3. Cross-reference: the audit report at `.planning/v1.1-MILESTONE-AUDIT.md` is the source of truth. Only mark what the audit confirms as complete.
  </action>
  <verify>
    <automated>cd /Users/ammarkashif/Documents/Code/allure-ai && grep -c "\- \[ \]" .planning/ROADMAP.md && grep "PLAY-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>All completed plan checkboxes in ROADMAP.md are marked [x]. PLAY-02 is marked [x] in REQUIREMENTS.md. Phase 9 plan entries are the only remaining unchecked items.</done>
</task>

</tasks>

<verification>
- Documents route compiles without TypeScript errors
- ROADMAP.md Phase 5/6 plans all show [x]
- REQUIREMENTS.md PLAY-02 shows [x]
- grep for "- [ ]" in ROADMAP.md returns only Phase 9 entries
</verification>

<success_criteria>
- Documents uploaded from post-recording dialog are forwarded for text extraction even when backendId arrives after a short delay
- All REQUIREMENTS.md checkboxes match audit findings
- All completed phase plan checkboxes marked in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-hardening-tech-debt/09-02-SUMMARY.md`
</output>


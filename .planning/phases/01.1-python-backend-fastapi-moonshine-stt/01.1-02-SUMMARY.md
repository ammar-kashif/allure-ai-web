---
phase: 01.1-python-backend-fastapi-moonshine-stt
plan: 02
subsystem: api
tags: [moonshine-voice, speechbrain, ecapa-tdnn, meanshift, stt, diarization, speaker-labeling]

# Dependency graph
requires:
  - phase: 01.1-python-backend-fastapi-moonshine-stt
    provides: FastAPI server shell with upload, status, job queue, and stubbed transcription worker
provides:
  - Moonshine Voice STT transcription (non-streaming)
  - SpeechBrain ECAPA-TDNN speaker diarization with MeanShift clustering (CPU-only)
  - Speaker-labeled merged transcript segments with stats
  - GET /recordings/{id}/transcript returns full TranscriptResponse JSON
affects: [01.1-03-PLAN]

# Tech tracking
tech-stack:
  added: [moonshine-voice, speechbrain, sklearn, torchaudio]
  patterns: [stt-diarization-pipeline, ecapa-tdnn-embeddings, meanshift-clustering, segment-merging]

key-files:
  created:
    - backend/transcription.py
    - backend/tests/test_transcription.py
  modified:
    - backend/main.py
    - backend/job_queue.py
    - backend/requirements.txt

key-decisions:
  - "Replaced pyannote.audio with SpeechBrain ECAPA-TDNN + MeanShift clustering (CPU-only, no gated models, no HF_TOKEN needed)"
  - "Moonshine Voice uses transcribe_without_streaming for simpler batch processing"

patterns-established:
  - "FastDiarizer pattern: SpeechBrain ECAPA-TDNN embeddings + sklearn MeanShift for CPU-only speaker clustering"
  - "Segment alignment: assign speaker by maximum time overlap between STT and diarization segments"
  - "Speaker filtering: remove speakers with <1% talk time before output"

requirements-completed: [BE-03, BE-04, BE-05]

# Metrics
duration: ~15min (across checkpoint pause)
completed: 2026-03-12
---

# Phase 1.1 Plan 02: Transcription Pipeline Summary

**Moonshine Voice STT with SpeechBrain ECAPA-TDNN diarization pipeline producing speaker-labeled, merged transcript segments via CPU-only inference**

## Performance

- **Duration:** ~15 min (includes checkpoint pause for user verification)
- **Started:** 2026-03-12T07:05:00Z
- **Completed:** 2026-03-12T09:32:41Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Complete STT + diarization pipeline in transcription.py with 7 composable functions
- SpeechBrain ECAPA-TDNN embeddings with MeanShift clustering for CPU-only speaker diarization (no GPU or gated model access required)
- Moonshine Voice non-streaming transcription producing timestamped text segments
- Speaker label remapping (SPEAKER_00 -> Speaker 1), consecutive segment merging, and <1% talk time filtering
- 6 unit tests for all pure utility functions (merge, remap, stats, alignment)
- End-to-end verification approved by user with real audio

## Task Commits

Each task was committed atomically:

1. **Task 1: Moonshine STT + SpeechBrain ECAPA-TDNN diarization pipeline with segment merging** - `10da969`, `dc31be9` (feat)
2. **Task 2: End-to-end transcription verification** - checkpoint:human-verify (approved, no code commit)

## Files Created/Modified
- `backend/transcription.py` - Core pipeline: STT, diarization, alignment, merging, stats, run_transcription entry point
- `backend/tests/test_transcription.py` - 6 unit tests for pure utility functions
- `backend/main.py` - Lifespan updated to load Moonshine transcriber and SpeechBrain diarizer at startup
- `backend/job_queue.py` - Replaced stub with real run_transcription import
- `backend/requirements.txt` - Added moonshine-voice, speechbrain, sklearn, torchaudio dependencies

## Decisions Made
- Replaced pyannote.audio with SpeechBrain ECAPA-TDNN + MeanShift clustering -- pyannote requires HF_TOKEN and gated model access; SpeechBrain ECAPA-TDNN is fully open, CPU-only, and requires no authentication
- Moonshine Voice uses transcribe_without_streaming (batch mode) rather than the chunked streaming API for simpler integration

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Replaced pyannote.audio with SpeechBrain ECAPA-TDNN**
- **Found during:** Task 1 (pipeline implementation)
- **Issue:** pyannote.audio requires HF_TOKEN for gated model access, not viable for CPU-only zero-config setup
- **Fix:** Implemented FastDiarizer class using SpeechBrain ECAPA-TDNN for speaker embeddings + sklearn MeanShift for clustering
- **Files modified:** backend/transcription.py, backend/main.py, backend/requirements.txt
- **Verification:** All 6 unit tests pass, end-to-end verification approved
- **Committed in:** dc31be9

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Essential replacement -- pyannote gated model requirement was a blocker. SpeechBrain provides equivalent functionality without authentication gates.

## Issues Encountered
None beyond the pyannote replacement documented above.

## User Setup Required
None - no external service configuration required. All models are open-access and download automatically on first run.

## Next Phase Readiness
- Full transcription pipeline operational end-to-end
- Audio upload -> queue -> transcribe -> diarize -> retrieve transcript flow complete
- Ready for Plan 03 to add any remaining features or move to Phase 2

## Self-Check: PASSED

All 5 key files verified present. Both task commits (10da969, dc31be9) verified in git log.

---
*Phase: 01.1-python-backend-fastapi-moonshine-stt*
*Completed: 2026-03-12*

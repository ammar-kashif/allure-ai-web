import { describe, it, expect } from "vitest"

describe("MermaidDiagram component", () => {
  it.todo("renders valid Mermaid code as SVG")
  it.todo("shows error fallback for invalid Mermaid syntax")
  it.todo("displays raw code in fallback when parse fails")
})
